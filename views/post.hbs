
<div class="col-lg-6">
  <div class="happywall">
    <ul>
      {{#each posts}}
        <img class="resize" src="{{image}}">
        <p class="handwritten">{{message}}</p>
      {{/each}}
    </ul>
  </div>
</div>
  {{>form}}
